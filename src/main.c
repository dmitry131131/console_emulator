#include "console.h"
#include "parser.h"

int main(int argc, char* argv[]) {

    // CommandsArray commands_array = {};

    // parse_commands(argc - 1, (argv + 1), &commands_array);

    // commands_array_dtor(&commands_array);

    console();

    return 0;
}